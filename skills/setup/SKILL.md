---
name: setup
description: Install and configure the managed Claudex gateway and stable launcher.
allowed-tools: Bash, AskUserQuestion
---

Run `${CLAUDE_PLUGIN_ROOT}/bin/claudex-pluginctl status` first. Explain that this
repository is currently a scaffold and that runtime setup is not implemented.
Never invent installation success, modify Claude Code settings directly, or
request credentials in chat. When setup is implemented, use the control CLI as
the single source of behavior and guide the user through a required relaunch.
